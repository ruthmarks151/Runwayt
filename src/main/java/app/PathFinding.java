package app;

/**
 * Finds the shortest and the second shortest path between two airports,
 * so that the user has a choice about which path is better.  These functions
 * both use breadth first search to find the path.  The breadth first search
 * should only happen once for the two functions.  Maybe they should be combined.
 * @author 
 *
 */
public class PathFinding {
	/**
	 * Finds the shortest path between two airports.
	 * @param airport
	 * @param airport
	 * @return
	 */
	public static FlightEvent[] getShortestPath(String airport, String destination) {
		FlightEvent[] flightPath = new FlightEvent[0];
		return flightPath;
	}
	
	/**
	 * Finds the second shortest path between two airports.
	 * @param airport
	 * @param destination
	 * @return
	 */
	public static FlightEvent[] getSecondPath(String airport, String destination) {
		FlightEvent[] flightPath = new FlightEvent[0];
		return flightPath;
	}
	
	
	
}
